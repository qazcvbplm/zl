spring:
  datasource:
    url: jdbc:mysql://172.17.0.3:3306/ops?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useUnicode=true&characterEncoding=utf-8&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
    password: TZH1234567890
    username: root
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
    driverClassName: com.mysql.jdbc.Driver
    filters: stat,wall,log4j
    initialSize: 2
    maxActive: 8
    maxPoolPreparedStatementPerConnectionSize: 20
    maxWait: 60000
    minEvictableIdleTimeMillis: 300000
    minIdle: 2
    poolPreparedStatements: true
    testOnBorrow: false
    testOnReturn: false
    testWhileIdle: true
    timeBetweenEvictionRunsMillis: 60000
    validationQuery: SELECT 1 FROM DUAL
  application:
    name: ops-dev
  redis:
    host: 192.168.0.1
    password: tzh1234567890
    port: 6379
  rabbitmq:
    host: 192.168.0.1
    port: 5672
    username: root
    password: tzh1234567890
    publisher-confirms: true
    publisher-returns: true
    listener:
      direct:
        acknowledge-mode: manual
      simple:
        acknowledge-mode: manual
barcode:
  path: /home/barcode
cert:
  path: /home/cert
eureka:
  client:
    serviceUrl:
      defaultZone: http://212.129.149.27:8761/eureka/
  instance:
    leaseExpirationDurationInSeconds: 2
    leaseRenewalIntervalInSeconds: 1
feign:
  hystrix:
    enabled: true
logging:
  file: logs/chuyin.log
  level:
    com:
      util: debug
  root: debug
ops:
  redis:
    cache: true
server:
  port: 12000
  compression:
    enabled: true
    mime-types: image/png,image/jpeg,image/jpg,text/html,application/javascript,text/css,application/octet-stream,application/json
    min-response-size: 1024
mybatis-plus:
  mapper-locations: ops/school/api/mapper/*.xml
  type-enums-package: ops/school/api/entity/enums

 